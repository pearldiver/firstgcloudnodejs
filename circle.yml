
general:
  build_dir: 1-hello-world
  
machine:
  node:
    version: 5.0.0
    
test:
  override:    
    - mocha test/index.js -t 30000

deployment:
  staging:
    branch: master
    commands:
      - gcloud -q preview app deploy app.yaml --promote --version=staging 