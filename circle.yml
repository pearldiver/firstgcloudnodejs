machine:
  node:
    version: 5.0.0
    
test:
  override:    
    - mocha 1-hello-world/test/index.js -t 30000

deployment:
  staging:
    branch: master
    commands:
      - cd 1-hello-world && gcloud config set project "myfirstnodeproj" && gcloud -q preview app deploy app.yaml --promote --version=staging 